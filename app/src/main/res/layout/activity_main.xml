<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        android:src="@drawable/main_background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <TextView
        android:id="@+id/main_tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="실종동물 찾기"
        android:textSize="45sp"
        android:fontFamily="@font/noto_bold"
        android:includeFontPadding="false"
        android:textColor="@color/colorWhite"
        android:layout_marginEnd="24dp"
        app:layout_constraintVertical_bias="0.27"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
    
    <ImageView
        android:id="@+id/main_iv_login"
        android:layout_width="136dp"
        android:layout_height="32dp"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/main_tv_title"
        app:layout_constraintEnd_toEndOf="@id/main_tv_title"
        android:src="@drawable/main_button"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="로그인"
        android:textSize="12sp"
        android:textColor="@color/colorWhite"
        android:fontFamily="@font/noto_medium"
        android:includeFontPadding="false"
        app:layout_constraintTop_toTopOf="@id/main_iv_login"
        app:layout_constraintBottom_toBottomOf="@id/main_iv_login"
        app:layout_constraintStart_toStartOf="@id/main_iv_login"
        app:layout_constraintEnd_toEndOf="@id/main_iv_login"/>

</androidx.constraintlayout.widget.ConstraintLayout>